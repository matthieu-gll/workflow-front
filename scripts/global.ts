import "../styles/global.scss"

console.log("global.ts => The purpose of this file is to be called across the entire application. For example we could imagine called here a web component that would allow the display of a modal for cookies.")